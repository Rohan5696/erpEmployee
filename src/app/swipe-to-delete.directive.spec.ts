import { SwipeToDeleteDirective } from './swipe-to-delete.directive';

describe('SwipeToDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeToDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
